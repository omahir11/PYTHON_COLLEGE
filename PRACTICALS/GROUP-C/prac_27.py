# j. Explore and use Python's built-in math module to perform the following:
# Calculate the square root of a number. 
# Compute the value of sine, cosine, and tangent for a given angle.


import math

num = 25

print("Square Root:", math.sqrt(num))

angle = math.radians(45)

print("Sin:", math.sin(angle))
print("Cos:", math.cos(angle))
print("Tan:", math.tan(angle))