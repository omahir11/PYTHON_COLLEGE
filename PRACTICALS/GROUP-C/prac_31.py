# n. Use the statistics module to calculate mean, median, and mode of a list of numbers.

import statistics

nums = [1, 2, 2, 3, 4, 5]

print("Mean:", statistics.mean(nums))
print("Median:", statistics.median(nums))
print("Mode:", statistics.mode(nums))